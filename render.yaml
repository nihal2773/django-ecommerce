services:
  - type: web
    name: syera-mini-mart
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python manage.py migrate && gunicorn config.wsgi"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9